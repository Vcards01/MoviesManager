<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoviesManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="backgroundColor">@color/backgroundColorDark</item>
        <item name="android:textColor">@color/white</item>
        <item name="materialButtonStyle">@color/white</item>
    </style>
    <style name="Button.Style" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>
</resources>